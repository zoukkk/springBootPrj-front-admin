# AGENTS.md - 后台管理系统前端开发规范

版本：v0.5  
说明：本文档用于约束 AI Agent 开发本后台管理系统前端时的行为，确保产出一致、可维护、可扩展。开始开发前必须先阅读本文档；如文档与用户在当前任务中的明确要求冲突，以用户的最新要求为准。

---

## 一、项目总体约束

### 1.1 技术栈

| 类别 | 选型 | 约束 |
| --- | --- | --- |
| 开发语言 | JavaScript | 当前项目不使用 TypeScript，不得擅自迁移为 TypeScript |
| 框架 | Vue 3 | 统一使用组合式 API 和 `<script setup>` |
| 构建工具 | Vite | 不得擅自更换 |
| UI 组件库 | Element Plus | 统一使用，禁止混用其他 UI 组件库 |
| 状态管理 | Pinia | 已接入，跨页面业务状态统一通过 Store 管理 |
| 路由 | Vue Router | |
| HTTP 请求 | Axios | 必须统一封装 |
| CSS 预处理器 | SCSS | 已接入，新建或改造样式统一使用 SCSS |
| 代码格式化 | Prettier | 按项目现有配置执行；没有配置时不得擅自大范围格式化旧代码 |

新增依赖前应确认它属于上述既定技术方案或确有必要。不得为了完成局部功能随意引入新的框架、UI 库或功能重复的依赖。

### 1.2 目录结构

```text
src/
├── api/          # 接口请求，按业务模块拆分
├── assets/       # 静态资源
├── components/   # 全局公共组件
├── composables/  # 组合式函数
├── layouts/      # 布局组件
├── router/       # 路由配置和守卫
├── stores/       # Pinia 状态
├── styles/       # 全局样式和 SCSS 公共变量
├── utils/        # 工具函数
├── views/        # 页面，按菜单和业务模块分目录
└── main.js
```

目录可以随实际开发逐步补充，不要求在尚未使用时提前创建空目录。

### 1.3 页面及接口路径

- 每个菜单按照业务层级建立目录。
- 系统管理下的用户管理页面使用 `src/views/system/user/index.vue`。
- 对应接口模块使用 `src/api/system/user.js`。
- Axios 请求封装统一放在 `src/utils/request.js`。
- 单页面模块可以直接使用 `index.vue`，同一模块的局部组件放在该模块的 `components/` 下。

### 1.4 命名规范

- Vue 组件名使用 `PascalCase`，例如 `UserTable.vue`。
- JavaScript 变量和函数使用 `camelCase`。
- 常量使用 `UPPER_SNAKE_CASE`。
- 接口函数使用明确的动作名称，例如 `getUserList`、`createUser`、`updateUser`、`deleteUser`。

### 1.5 代码风格

- Vue 组件统一使用 `<script setup>`。
- 禁止在模板中编写复杂逻辑，应抽取到 `computed`、组合式函数或方法中。
- 优先复用现有组件和工具，不重复实现相同能力。
- 修改应围绕当前需求展开，不进行无关重构和大范围样式改写。

---

## 二、UI 与交互规范

### 2.1 布局

- 后台采用经典布局：左侧菜单 + 顶部导航 + 内容区。
- 内容区统一内边距为 `20px`，特殊页面需要调整时应说明原因。
- 业务页面统一使用 `src/components/PageContainer.vue` 提供页面标题、说明、操作区和内容外壳。
- 后台框架组件统一放在 `src/components/layout/`，侧栏、顶部栏、面包屑和内容区不得重复写回业务布局组件。

### 2.2 通用交互

- 列表页通常由搜索区、操作区、表格区和分页区组成；没有搜索或分页需求时可以省略对应区块。
- 所有删除操作必须通过 `ElMessageBox.confirm` 二次确认。
- 所有异步提交按钮必须具有 loading 状态，并提供成功或失败反馈。
- 表单使用 Element Plus 表单校验规则。
- 空状态使用统一空状态组件；公共组件尚未建立时，应在首次需要时封装。
- 表格加载使用 `v-loading`，按钮加载使用 `loading` 属性。
- 页面在常用桌面尺寸和移动端窄屏下不得出现明显溢出、遮挡或不可操作问题。

### 2.3 反馈提示

- 成功提示使用 `ElMessage.success(...)`。
- 失败提示使用 `ElMessage.error(...)`，优先展示后端返回的有效错误信息。
- 确认操作使用 `ElMessageBox.confirm(...)`。
- 提示文案应说明具体结果，例如“用户创建成功”，不应在有明确上下文时只写“操作成功”。

---

## 三、请求与数据规范

### 3.1 Axios 封装

- Axios 实例统一放在 `src/utils/request.js`。
- 所有接口模块必须引入该请求实例，不得在页面组件中重复创建 Axios 实例。
- 接口模块按业务目录拆分，例如 `src/api/system/user.js`。
- 统一配置 `baseURL` 和超时时间。
- 请求拦截器自动携带 token。
- 响应拦截器统一处理业务码：`code === 0` 表示业务成功。
- 收到 401 时清理失效登录状态并跳转登录页，同时避免重复跳转和重复提示。
- 错误默认通过 `ElMessage.error` 统一提示；调用方需要特殊处理时应保留错误继续抛出。

接口模块示例：

```js
import request from '@/utils/request'

class UserApi {
  static getUserList(params) {
    return request({
      url: '/system/user/list',
      method: 'get',
      params,
    })
  }
}

export default UserApi
```

### 3.2 返回数据结构

后端统一以 `code === 0` 表示成功：

```js
{
  code: 0,
  message: 'success',
  data: null,
}
```

分页数据统一放在 `data` 中：

```js
{
  list: [],
  total: 0,
  page: 1,
  pageSize: 10,
}
```

如实际接口暂时不符合约定，应在对应接口适配层处理，并明确记录差异，不得让页面散落兼容逻辑。

### 3.3 Token 与主题存储

- 运行中的访问令牌以 Pinia 和内存 Token 服务为准，请求拦截器不得在每次请求时重新读取 `localStorage`。
- 勾选“记住我”时将 Token 写入 `localStorage`；未勾选时写入 `sessionStorage`。
- 收到后端 401 或用户主动退出时，统一清理内存、`localStorage` 和 `sessionStorage` 中的 Token。
- 主题状态使用独立的 `app-theme` 键保存在 `localStorage`，不得与登录状态耦合。
- 登录页和后台布局必须使用同一个主题 Store，确保登录前后主题连续一致。

---

## 四、路由与权限规范

### 4.1 权限方案

- 前端维护完整业务路由、页面组件映射和路由元信息；不得直接执行后端返回的组件字符串。
- 登录后调用 `/api/auth/menus` 获取当前用户的授权菜单树，并与前端路由配置匹配后展示。
- 使用 `meta.roles` 声明允许访问该路由的角色；最终权限取 `meta.roles` 与后端授权菜单的交集。
- 菜单显示和直接路由访问必须使用同一套组合权限判断，不能只隐藏菜单。
- `meta.roles` 为空或未设置时，是否允许所有已登录用户访问应由统一权限函数处理，页面内不得各自判断。

路由元信息示例：

```js
meta: {
  title: '首页',
  icon: 'HomeFilled',
  requiresAuth: true,
  roles: ['admin', 'user'],
}
```

### 4.2 路由组织

```text
src/router/
├── index.js          # 创建 Router 实例
├── static-routes.js  # 登录、404、403 等公共路由
├── async-routes.js   # 前端维护的完整业务路由表
└── guard.js          # 登录鉴权、角色过滤和路由注入
```

上述结构在路由功能扩展时逐步落地，不要求为了目录一致性提前创建无内容文件。

- 未登录访问受保护页面时重定向到登录页。
- 已登录但角色无权访问时跳转到 403 页面。
- 刷新页面后必须能够恢复登录状态和有权限的路由。
- 直接输入无权限地址时也必须执行权限校验，不能只隐藏菜单。

---

## 五、开发与交付流程

### 5.1 开发顺序

采用逐个页面递进开发的方式。每完成一个页面，先由 Agent 自检，再交给用户检查；未确认前不主动进入下一阶段。

当前阶段规划：

1. 登录页。
2. 主布局和首页 Dashboard。
3. 用户管理 CRUD。
4. 角色与权限管理。
5. 其他业务模块 CRUD。
6. 根据重复使用情况沉淀表格、表单、弹窗等公共组件。

### 5.2 开发前说明

每个页面开始开发前，Agent 应简要说明：

- 页面包含哪些区块。
- 使用哪些接口。
- 使用哪些现有或待新增的公共组件。
- 主要交互和异常状态如何处理。

### 5.3 Agent 自检

开发完成后，在可合理验证的范围内至少完成以下检查：

- 页面能够持续正常运行，不存在已知的阻塞性错误、死循环或明显控制台异常。
- 核心交互、权限、loading、空状态和错误处理符合本规范。
- 命名、目录和代码组织符合本规范。
- 执行项目构建命令并确认 build 成功；当前项目默认使用 `npm run build`。
- 对本次改动涉及的页面进行必要的手动验证；存在自动化测试时运行相关测试。
- 如果因环境、后端服务或其他客观原因无法完成某项验证，交付时必须明确说明，不得将未验证描述为已通过。

### 5.4 用户验收

- Agent 自检通过后将结果交给用户检查。
- 只有当用户明确回复当前版本“验收通过”时，才视为完成验收。
- 用户未明确回复“验收通过”前，不得自行更新该版本的验收记录，也不得假设已经验收。
- 用户提出修改意见时，继续在当前版本范围内调整并重新自检。

---

## 六、版本记录规范

- 验收通过后，将本次版本变更写入根目录 `VERSION.md`。
- 初始产品版本从 `v1.0.0` 开始。
- 版本号采用 `主版本.次版本.修订版本`，具体升级级别由 Agent 根据改动范围判断。
- 主版本：架构或产品方向发生不兼容的重大变化。
- 次版本：新增完整一级菜单、完整业务模块或影响范围较大的功能。
- 修订版本：新增菜单内页面、局部功能、样式调整、问题修复和其他兼容性改动。
- 同一验收批次包含多类改动时，以其中影响最大的改动确定版本号。

记录格式：

```text
v1.0.0 feat: 新增登录页面完整逻辑 2026年9月17日
```

常用类型可使用 `feat`、`fix`、`style`、`refactor`、`docs`。版本记录应简洁说明用户可感知的主要变化。
