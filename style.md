# 文章管理后台视觉规范

## 目标与边界

将现有界面统一为克制、专业、适合高频内容维护的“内容运营工作台”。优先保证文章列表的扫描效率、编辑任务的清晰度，以及跨页面的一致性。

本轮改造必须保持以下边界：

- 保持 Vue 3、Vue Router 和 Element Plus。
- 不修改现有接口、请求参数、路由地址及业务数据结构。
- 不引入新的 UI 框架或图标库；继续使用 `@element-plus/icons-vue`。
- 不新增装饰性大图、渐变背景、玻璃质感或营销页式卡片布局。

## 视觉方向

从“玻璃质感 SaaS”收束为“内容运营工作台”：浅灰工作区、纯白操作面、深蓝主色、低饱和状态色。信息层级通过留白、边框、字重和背景区分，不依赖渐变、模糊或重阴影。

### 设计原则

1. 一个页面只有一个主操作，例如“新增文章”。
2. 表格、筛选和编辑是后台的核心，视觉上优先于装饰元素。
3. 常规面板使用边框分层；仅弹窗、下拉菜单等浮层使用轻阴影。
4. 状态不可只依赖颜色，必须保留明确文字，例如“已发布”“草稿”。
5. 动效只用于状态反馈，且应短促、可关闭、不改变布局尺寸。

## 设计 Token

### 颜色

| 语义 | 值 | 用途 |
| --- | --- | --- |
| 工作区背景 | `#F6F7F9` | 应用主背景 |
| 表面 | `#FFFFFF` | 页面面板、表格、输入控件 |
| 表面弱化 | `#F9FAFB` | 筛选区、表头、悬停前的辅助区 |
| 主文字 | `#1F2937` | 标题、正文、主要数据 |
| 次级文字 | `#667085` | 说明、表头、辅助信息 |
| 弱化文字 | `#98A2B3` | 占位、非关键元数据 |
| 边框 | `#EAECF0` | 面板、表格、控件边界 |
| 分隔线 | `#F2F4F7` | 表格行、内容分组 |
| 主色 | `#2563EB` | 主按钮、当前导航、链接、焦点环 |
| 主色浅底 | `#EFF6FF` | 当前导航、选中行、轻提示 |
| 发布状态 | `#067647` / `#ECFDF3` | 已发布文字 / 标签背景 |
| 草稿状态 | `#475467` / `#F2F4F7` | 草稿文字 / 标签背景 |
| 警告状态 | `#B54708` / `#FFFAEB` | 待处理或提示信息 |
| 危险状态 | `#B42318` / `#FEF3F2` | 删除、错误信息 |

禁止在业务组件中散落原始色值。优先通过全局 CSS 自定义属性和 Element Plus CSS 变量表达以上语义。

### 字体与排版

- 字体：`Inter, "PingFang SC", "Microsoft YaHei", sans-serif`。
- 页面标题：24px / 32px / 600。
- 区块标题：18px / 28px / 600。
- 正文和表格内容：14px / 22px / 400。
- 表单标签、表格表头、按钮：14px / 20px / 500。
- 辅助文字：12px / 18px / 400。
- 数字、时间、统计值使用 `font-variant-numeric: tabular-nums`，避免列宽跳动。
- 不使用紧缩字距；标题与正文保持自然换行。

### 间距、圆角与层级

- 采用 4px 基数：4、8、12、16、20、24、32px。
- 页面内容区：24px；移动端：16px。
- 标题区与后续内容：24px；模块之间：20-24px；控件之间：8-12px。
- 页面面板圆角：8px；输入框、按钮、标签圆角：6px。
- 常规面板无投影，仅使用 1px 边框；弹窗和下拉浮层可使用轻阴影。
- 不使用大圆角、渐变圆球、背景光斑、毛玻璃及半透明表格。

### 交互状态

- 所有交互控件提供 hover、active、focus、disabled 状态。
- 焦点环：2px 主色外环，不能移除默认键盘焦点且不提供替代。
- 常规状态切换使用 150-200ms 的 `opacity`、`color`、`background-color`、`box-shadow` 或 `transform`；禁止用宽高动画造成布局抖动。
- 遵从 `prefers-reduced-motion`，在减少动态偏好下取消非必要过渡。
- 异步保存按钮进入 loading 状态并禁止重复提交。

## 应用框架

### 侧栏

- 固定浅色导航面，不使用毛玻璃和圆角边缘。
- 宽度维持现有桌面尺寸；品牌区高度约 56px。
- 当前菜单项使用 3px 左侧主色标记、`#EFF6FF` 浅底和主色文字；不使用重阴影。
- 常规菜单文字为次级文字，悬停使用浅灰背景。
- 图标与文字保持 8px 间距，图标风格统一使用 Element Plus 线性图标。
- 移动端不可只留下无文字图标。应改为可展开导航或顶部菜单，确保导航项可发现。

### 顶栏

- 白色背景，底部 1px 分隔线；不使用毛玻璃。
- 高度约 64px，左右内边距 20-24px。
- 面包屑为辅助信息，弱化处理；用户入口保留头像、姓名与下拉箭头。
- 移除“毛玻璃开关”。明暗主题如保留，应成为产品级设置，而非局部视觉效果开关。

### 页面面板

- 每个路由页面使用单一白色主面板，`padding: 24px`，1px 边框，8px 圆角。
- 页面标题区左侧为标题和一句用途说明，右侧只放该页唯一的主操作。
- 不将页面区块继续包裹为多层悬浮卡片；筛选区、表格区通过背景和边线区分。

## 文章管理页

文章管理页是全站的标准模板，分类和用户页面应复用其层级、工具栏和弹窗规则。

### 标题区

- 标题：“文章管理”；说明保留现有文案。
- 右侧“新增文章”为唯一实色主按钮，带现有 `Plus` 图标。
- 不在标题区加入装饰数据、渐变或多余的状态标签。

### 筛选区

- 作为独立浅灰区域，背景 `#F9FAFB`，边框 `#EAECF0`，内边距 12-16px。
- 标题搜索框桌面宽度约 280px；状态选择器约 140px；移动端全宽并垂直排列。
- “删除选中”属于危险次级操作，仅在有选中项时启用；可显示选中数量。
- 有筛选条件但无结果时，显示“没有匹配的文章”并提供“清除筛选”动作。

### 表格

- 使用不透明白底；表头背景 `#F9FAFB`，高度 44px；行高约 52px。
- 行之间使用 `#F2F4F7` 分隔；悬停背景使用极浅蓝或灰色。
- 标题列优先保证宽度；长标题截断时必须保留 tooltip 或可查看完整内容的路径。
- 时间列和统计数字使用等宽数字。
- “已发布”“草稿”使用低饱和标签：浅背景、深文字、6px 圆角。
- 右侧操作列固定。图标按钮必须有 tooltip 与 `aria-label`；编辑可保留图标操作。
- 表格为空时使用 Element Plus 空状态，并提供“新增文章”入口。

### 分页

- 分页在表格下方右对齐，与表格保持 16-20px 间距。
- 当前页使用主色，非当前页保持中性表面和清晰边界。

### 编辑弹窗

- 宽度沿用现有约束；移动端宽度不超过视口并保留安全边距。
- 内容按“基本信息”和“正文内容”分组，但不改变现有字段、数据结构和保存流程。
- 所有字段保留可见标签，必填项有明确标识；错误信息显示在字段附近。
- 底部操作右对齐：取消为次级按钮，保存为主按钮并带 loading 状态。
- 关闭存在未保存更改的表单时，应在后续业务完善时增加确认提示。

## 分类、用户与仪表盘

### 分类管理

- 复用文章页标题区、表格、弹窗和操作样式。
- 删除为危险操作，应增加二次确认；确认文案需说明将删除的分类名称及影响范围。

### 用户管理

- 个人资料展示使用信息列表或描述列表，但应统一为白底、细分隔线、清晰标签列。
- “编辑资料”使用页面唯一主按钮；头像地址等辅助字段提供说明文字，不改变现有接口。

### 仪表盘

- 指标卡统一使用中性色图标容器，不为每张卡分配高饱和品牌色。
- 仅在确有业务含义时使用发布、警告、危险等语义色。
- 最近动态以时间线或分隔列表呈现，信息按“动作、对象、时间”扫描；避免额外的浮层卡片。
- 快捷入口保持单一主操作，不与页面主操作竞争。

## 状态、反馈与可访问性

- 删除文章、删除分类、批量删除都必须使用确认弹窗；确认按钮采用危险样式。
- 成功提示简洁且不抢焦点；失败提示需说明原因和可采取的下一步动作。
- 图标按钮、主题切换、用户菜单等必须提供可访问名称。
- 表单不使用仅靠 placeholder 的标签；字段错误放在对应字段附近。
- 不可仅用颜色传达状态；标签中需保留文字。
- 文本与背景对比度至少达到 4.5:1；交互控件焦点状态与邻近颜色至少达到 3:1。
- 在 375、768、1024、1440px 下检查标题、筛选区、表格横向滚动、弹窗和导航可用性。

## 实施顺序

1. 在全局样式中建立语义 token，并映射 Element Plus 主题变量。
2. 收束应用背景、侧栏、顶栏和页面面板，移除玻璃效果和渐变背景。
3. 以文章管理页为模板改造标题区、筛选区、表格、分页与弹窗。
4. 将相同规范复用至分类、用户和仪表盘页面。
5. 补齐删除确认、空状态、加载状态、焦点样式和移动端导航。

## 验收条件

- Vue 3、Element Plus、现有路由与接口行为保持不变。
- 不再出现渐变背景、毛玻璃、透明表格或与工作台无关的装饰效果。
- 所有页面使用同一套颜色、间距、圆角、按钮、表格和弹窗规则。
- 每页只存在一个视觉明确的主操作。
- 删除类操作有确认，表单保存有 loading，空数据与筛选空结果有明确反馈。
- 键盘焦点可见，图标操作有文字替代，状态信息不单靠颜色表达。
